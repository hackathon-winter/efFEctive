# ========================================
# 共通（開発・本番 両方で必要なパッケージ）
# ========================================
Django>=5.2,<6.0
django-bootstrap5>=25.0
django-environ>=0.12.0
django-extensions>=4.0
requests>=2.32

# ========================================
# データベースドライバ（どちらか片方を有効にする）
# ----------------------------------------
# 開発環境向け：ビルド不要で手軽（ただし本番非推奨）
psycopg2-binary>=2.9,<3.0

# 本番環境向け：安定性・メモリ効率に優れる（開発環境ではビルド必要）
# psycopg2>=2.9,<3.0
# 本番環境では上を有効にし、`psycopg2-binary` をコメントアウト

# ========================================
# アプリケーションサーバ（本番で使うもの）
# ----------------------------------------
# 開発環境では runserver を使うため不要
# 本番環境では以下をコメントアウト解除して使う
uwsgi>=2.0

# ========================================
# 開発専用ツール（本番では絶対に使わない）
# ----------------------------------------
# ipython
# django-debug-toolbar
