from django.urls import path
from . import views

urlpatterns = [
    #path('view/', views.view_progress, name='view_progress'),
    #path('reset/', views.reset_progress, name='reset_progress'),
    #path('update/', views.update_progress, name='update_progress'),
]
